# HydroSense Command

Municipal flood monitoring and response frontend for HydroSense.

## Local development

From this directory, install the project dependencies and run the Vite development server:

```bash
npm install
npm run dev
```

The application uses the HydroSense logo and favicon assets in `public/branding/` and `public/favicon.svg`.
