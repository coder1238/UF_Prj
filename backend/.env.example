DATABASE_URL=sqlite:///./app.db
JWT_SECRET=replace-this-in-production
SIMULATION_TICK_SECONDS=8
# Add deployed production frontend URLs here (for example, Vercel deployment URLs), comma-separated.
ALLOWED_ORIGINS=
